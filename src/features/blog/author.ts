/**
 * 블로그 글쓴이 표기.
 *
 * 글은 관리자 계정으로 쓰지만, 읽는 사람에게는 개인이 아니라 서비스가 쓴 글이다.
 * 서버가 내려주는 authorNickname을 그대로 쓰면 운영자가 바뀔 때마다 글쓴이 이름이 달라지고,
 * 개인 닉네임이 공개 페이지에 그대로 노출된다. 그래서 화면·검색 구조화 데이터 모두
 * 이 값 하나로 통일한다.
 */
export const BLOG_AUTHOR = '유니로드';
